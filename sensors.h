#ifndef SENSORS_H
#define SENSORS_H

long Ping(int pin) {
  // Optional: add ultrasonic sensor support.u can add more sensors as needed.can be customized according to your requirements.
  return 0;
}

#endif
